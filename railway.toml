[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/app/start.sh"
# Socket Mode has no inbound HTTP, so there is no healthcheck endpoint — Railway
# treats the long-running worker as healthy while the process is up.
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
